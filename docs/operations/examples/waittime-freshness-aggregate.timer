# Copy/adapt example only. Enable only on a trusted private runner.

[Unit]
Description=Refresh Wait Time Canada daily analytics aggregates

[Timer]
OnCalendar=*-*-* 06:10:00
Timezone=UTC
Persistent=true
Unit=waittime-freshness-aggregate.service

[Install]
WantedBy=timers.target
