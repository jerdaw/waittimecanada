# Wait Time Canada - Frontend Environment Variables
# Copy this to .env.local and fill in your values

# Canonical public base URL (used for sitemap/robots/metadata)
NEXT_PUBLIC_BASE_URL=https://wait-time.ca

# Mapbox API Token (required for map display)
# Get yours at: https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_token_here

# Neon PostgreSQL Connection String (required for API routes)
# Format: postgresql://username@host/database?sslmode=require (password omitted in examples)
# Get yours from: https://console.neon.tech/
DATABASE_URL=postgresql://username@ep-something.region.aws.neon.tech/neondb?sslmode=require
